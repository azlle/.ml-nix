abbr "la"="eza -glBa"
abbr "ln"="eza -gT $(nix build nixpkgs#§ --no-link --print-out-paths)"
abbr "mtsaigyo"="sudo mkdir -p /mnt/saigyouji && sudo mount -t btrfs -o nossd,noatime,compress=zstd:3,autodefrag,space_cache=v2 LABEL=saigyouji /mnt/saigyouji && sudo chown $(id -u):$(id -g) /mnt/saigyouji"
abbr "mtmelchi"="sudo mkdir -p /mnt/melchior && sudo mount -t btrfs -o ssd,noatime,compress=zstd:3,discard=async,space_cache=v2 LABEL=melchior /mnt/melchior && sudo chown $(id -u):$(id -g) /mnt/melchior"
abbr "nrn"="sudo nixos-rebuild switch§ --flake ~/.nix_ml#necrofantasia"
abbr "hms"="home-manager switch§ --flake ~/.nix_ml#sumizomenosakura"
abbr "nst"="nix store gc && nix store optimise"
abbr "ngl"="sudo nix-env --list-generations --profile /nix/var/nix/profiles/system"
abbr "ngd"="sudo nix-env --delete-generations +3§ --profile /nix/var/nix/profiles/system"
abbr "hgl"="nix-env --list-generations --profile ~/.local/state/nix/profiles/home-manager"
abbr "hgd"="nix-env --delete-generations +3§ --profile ~/.local/state/nix/profiles/home-manager"
abbr "ydla"="nix run --refresh nixpkgs/nixpkgs-unstable#yt-dlp -- -x --audio-format flac"
abbr "ydlv"="nix run --refresh nixpkgs/nixpkgs-unstable#yt-dlp -- -f 'bv*+ba/b' --merge-output-format mkv§ --postprocessor-args 'Merger:-c:a pcm_s16le' §"
abbr "gdl"="nix run --refresh nixpkgs/nixpkgs-unstable#gallery-dl -- --dest ~/gdl_dest/`date +%y%m%d`/"
abbr "a"="git add"
abbr "c"="git commit"
abbr "C"="git checkout"
abbr "CCC"="git reflog expire --expire=now --all && git gc --prune=now --aggressive"
abbr "f"="git status"
abbr "ff"="git log --graph --date=iso --pretty='format:%C(yellow)%h %C(cyan)%ad %C(green)%an%Creset%x09%s %C(red)%d%Creset'"
abbr "fff"="git branch -v§"
abbr "F"="git fetch"
abbr "s"="git submodule"
abbr "p"="git pull"
abbr "P"="git push"
